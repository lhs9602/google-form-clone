import { surveyProps } from "../../data/Type";
import { BoxContainer } from "../editBox/EditBox.styled";

export const TextPreviewBox = ({ question }: surveyProps) => {
  return <BoxContainer></BoxContainer>;
};
